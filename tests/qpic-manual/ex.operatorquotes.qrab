circuit manual_backend_operator {
  backend latex {
    before: "\\node[draw=red,circle] at (0,0.6) {raw};"
  }
  backend typst {
    before: "#circle(stroke: red)[raw]"
  }
  qubit q
  gate "portable" on q
}
