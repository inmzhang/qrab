circuit manual_hyper_target {
  backend latex {
    preamble: "\\usepackage{hyperref}"
    before: "\\hypertarget{manual-target}{}"
  }
  backend typst {
    before: "#metadata(\"manual-target\") <manual-target>"
  }
  qubit q
  h q
}
