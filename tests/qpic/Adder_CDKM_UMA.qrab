circuit adder_cdkm_unmajority {
  qubit a: "a xor c" -> "a"
  qubit b: "b xor c" -> "sum"
  qubit c: "majority" -> "c"

  gate "UMA" on a, b, c
  equals
  reverse {
    x c if a, b
    x a if c
    x b if c
  }
  equals
}
