circuit controlled_swap {
  qubit control: "c"
  qubit left: "a" -> "b"
  qubit right: "b" -> "a"

  gate "SWAP" on left, right if control with fill: "#F3E8FF", stroke: purple
  equals "Fredkin" on control, right braced right
  x left if control
  x right if left, control
  x left if control
}
